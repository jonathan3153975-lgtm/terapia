name: terapia_patient_app
description: App Flutter do paciente para a plataforma Terapia.
publish_to: 'none'
version: 1.0.2+4

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.2.4
  http: ^1.2.1
  intl: ^0.19.0
  just_audio: ^0.10.4
  package_info_plus: ^8.0.2
  shared_preferences: ^2.5.3
  timezone: ^0.9.4
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true